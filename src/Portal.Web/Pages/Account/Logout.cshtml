@page
@model Portal.Web.Pages.Account.LogoutModel
@{
    // Разметка не нужна: страница только обрабатывает POST и делает перенаправление.
    // Сам файл .cshtml всё равно обязан существовать — по нему Razor Pages строит маршрут.
}
